import { ToolPlaceholder } from "../_components/tool-placeholder";

export default function CgpaConverterPage() {
  return (
    <ToolPlaceholder titleKey="calculator.cgpa.title" subtitleKey="calculator.cgpa.subtitle" />
  );
}
